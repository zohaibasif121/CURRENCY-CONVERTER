 // exchangeRatesDiv.innerHTML = `<h2>Base Currency: ${data.base_code}</h2>`;
            // const rates = data.conversion_rates;
            // const ratesList = Object.keys(rates).map(currency => 
            //     `<p>${currency}: ${rates[currency]}</p>`
            // ).join('');
            // exchangeRatesDiv.innerHTML += ratesList;
           